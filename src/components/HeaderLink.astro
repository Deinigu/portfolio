---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { href = "/", class: className = "", ...props } = Astro.props;

const currentPath = Astro.url.pathname;

// Normaliza eliminando slash final (si tiene) para comparar bien
const normalizePath = (path: string) => path.replace(/\/$/, "");

const normalizedCurrent = normalizePath(currentPath);
const hrefString = href?.toString() || "/";
const normalizedHref = normalizePath(hrefString);

const isRootPath = normalizedHref === "/portfolio";
const isActive = isRootPath
  ? normalizedCurrent === normalizedHref
  : normalizedCurrent === normalizedHref;
---

<a
  href={href}
  class={`${className} bg-ctp-surface1 text-ctp-text hover:bg-ctp-overlay2 hover:text-ctp-base m-1.5 flex rounded-3xl p-3 transition-all duration-200 hover:scale-110 active:scale-95 ${
    isActive ? "active" : ""
  }`}
  {...props}
>
  <slot />
</a>

<style>
  a {
    display: inline-block;
    text-decoration: none;
  }

  a.active {
    background: var(--color-ctp-blue);
    color: var(--color-ctp-base);
  }
</style>
