---
import type { HTMLAttributes } from "astro/types";
type Props = {
  collection: string;
  post: {
    id: string;
    data: {
      title: string;
      description: string;
      pubDate: string;
      heroImage: string;
    };
  };
} & HTMLAttributes<"a">;
const { collection, post, ...props } = Astro.props;

// Base URL sin barra final (ej: "/portfolio")
const base = import.meta.env.BASE_URL.replace(/\/$/, "");

// Ruta final corregida
const finalHref = `${base}/${collection}`;
---

<div
  class="w-full h-full flex flex-col rounded-2xl shadow-lg hover:shadow hover:scale-105 transition-all duration-300 bg-ctp-base"
>
  <a href={`${finalHref}/${post.id}`} class="block no-underline h-full">
    <img
      class="w-full aspect-video object-cover rounded-xl"
      src={post.data.heroImage}
      alt={`Image of ${post.data.title}`}
    />
    <div class="px-6 py-4">
      <div class="font-bold text-xl mb-2">{post.data.title}</div>
      <div class="cut-text text-base h-2xl max-h-24 overflow-hidden">
        {post.data.description}
      </div>
    </div>
  </a>
</div>

<style>
  .cut-text {
    display: -webkit-box;
    -webkit-line-clamp: 3; /* Número de líneas a mostrar */
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
